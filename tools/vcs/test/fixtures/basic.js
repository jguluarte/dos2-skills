// Basic density cases
foo(bar());
callback(obj.method());
wrap(parse(data)).unwrap();
const msg = `${getName()}`;
const plain = `${name}`;
skills.forEach(skill => renderCard(skill));
obj[arr[index]];
obj[arr[longPropertyName]];
