export class Investment {
    lablel = "Investment";
    compare = (a, b) => a.investment - b.investment;
}

class OtherTree {

}


function apply(skills) {
    return this.filter.sorting.find
}

skills.sort( strategySort );

const strategySort = (a, b) => filter.sorting.map(
    (s) => s(a, b)
).find((r) => !!r);

filter.sorting.map((s) => s(a, b)).find()
