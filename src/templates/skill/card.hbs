<skill-card
    data-trees="{{secondaryTree}},{{primaryTree}}"
    data-primary-tree="{{lowercase primaryTree}}"
    data-secondary-tree="{{lowercase secondaryTree}}"
>

    {{~> skill-card-body ~}}

</skill-card>
